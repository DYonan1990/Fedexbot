{
  "name": "fedex-quickship",
  "version": "0.3.0",
  "private": true,
  "description": "Deterministic parser that turns messy ServiceNow tickets into validated FedEx shipments. The parser lives in app/fedex_bot.html; the golden dataset in tickets/ is the contract.",
  "scripts": {
    "test": "node tests/run-regression.js",
    "test:verbose": "node tests/run-regression.js --verbose"
  },
  "license": "UNLICENSED"
}
